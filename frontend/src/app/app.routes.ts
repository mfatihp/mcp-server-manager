import { Routes } from '@angular/router';
import { DashboardPageComponent } from './features/dashboard/dashboard.module';

export const routes: Routes = [
    { path: 'dashboard', component: DashboardPageComponent }
];
